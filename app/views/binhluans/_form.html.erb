<%= form_for Binhluan.new do |f| %>
  <%= f.hidden_field :masp, value: @sanpham.id %>

  <div class="form-group">
    <%= f.text_field :noidung, class: "form-control", required: true %>
  </div>

  <div class="form-group">
    <%= f.submit "Bình luận", class: "btn btn-primary" %>
  </div>
<% end %>
